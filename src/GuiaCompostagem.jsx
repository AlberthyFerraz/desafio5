export default GuiaCompostagem;  // ← Isso é essencial!

// O componente deve ser definido como:
function GuiaCompostagem() {
  return (
    <div>
        digite aqui
      {/* conteúdo do componente */}
    </div>
  );
}